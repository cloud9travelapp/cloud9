@AGENTS.md

# Project handoff — Cloud9

AI travel-planning web app. Next.js 16 (App Router, Turbopack) · React 19 · TypeScript · Tailwind v4 · NextAuth v5 (Google → Supabase) · Anthropic SDK (claude-sonnet-5 concierge, claude-haiku-4-5 namer). Repo `cloud9travelapp/cloud9`, deploys to Vercel from `main`. Mock providers need no env vars.

## Architecture (key patterns)
- **Chat**: streaming route `app/api/chat/route.ts`. The system prompt IS the concierge's behavior spec. Tool round-trip loop supports multiple tools; auth-gated; saves to Supabase. Model history window = the LATEST 40 messages (desc + reverse — taking the oldest was the "context bleed" bug; same for the 200-message display window in `app/chat/page.tsx`). Pre-tool text is declared USER-VISIBLE in the prompt (tool_choice:auto lets the model emit chain-of-thought before tool_use — the documented, prompt-side-handled narration-leak cause; remember_preference calls carry no text at all).
- **Runtime diagnosis = SQL, not logs**: `lib/diag.ts` `logDiag(kind, detail)` → Supabase `diag_events` (best-effort, console-mirrored). Instrumented: every tool_call (+outcome), search errors (real cause), stays quota fallback, stream errors, title errors. Vercel's ~1h log retention makes log-hunting a dead end — query `diag_events` instead. Extend this pattern for any new "what happened at runtime?" question.
- **Trip titles**: `deriveTripTitle` (haiku) runs each turn via `after()` (never blocks the stream) — returns a title covering ALL destinations ("Japan & Korea") or KEEP (defined strictly: nothing new named, nothing changed — the old "no destination known yet" phrasing made haiku KEEP first turns). While a trip is "New Trip" the titler sees the last ~5 user messages (self-healing). A user rename (pencil in the sidebar → `PATCH /api/trips/[id]`) sets `name_is_custom`, which locks the title against auto-updates. Both paths degrade gracefully if the `name_is_custom` column is missing. Titles use the SHORTEST natural name (never "City, Country"; origin city is never a destination) and pass through the deterministic `sanitizeTripTitle` guard (`lib/trip-title.ts`, unit-tested) before storage.
- **Prompt caching**: the concierge system prompt = `systemStatic` (persona + prefs + date + ALL behavior rules; `cache_control: ephemeral`; byte-identical across a user's turns) + `systemDynamic` tail (per-turn language directive, first/returning line). NEVER interpolate per-turn text into the static block — it kills the cache. Each turn logs `chat timing: pre-model/first-token/total` to the runtime logs.
- **Reply language is computed, never model-inferred**: `lib/language.ts` decides each turn's language (word-dominance of the latest user message; place/brand names don't flip it; ambiguous → conversation's established language; new+ambiguous → Hebrew, the onboarding-preference seam) and the route injects it as a hard per-turn directive covering pre-tool notes, tool summaries, and block strings. Don't add prompt language rules that re-infer from "the user's message" — that was the root cause of the mixed-language bugs.
- **Provider-agnostic search** (real provider = one-file swap): `lib/flights/*` (`search_flights`) and `lib/stays/*` (`search_stays`). Each = types + a `searchX` switch on `X_PROVIDER` env (default `"mock"`, exports `IS_MOCK_*`) + a seeded mock. Don't change existing provider signatures.
- **Known demo limitation — TEST-environment inventory tops out at ~4★** (confirmed 2026-07-21: the cached Paris fetch's star histogram had no 5★; our tercile/star filtering was clean). The luxury band can't show what Hotelbeds test inventory doesn't carry; the prompt's inventory-honesty rule attributes the gap to "my current inventory", never to the city. Re-check when production keys arrive.
- **Hotelbeds provider (LIVE since 2026-07-20)**: `lib/stays/hotelbeds.ts` — APItude geolocation availability search (the concierge passes lat/lng in the tool call, like IATA codes); active via `STAY_PROVIDER=hotelbeds` + `HOTELBEDS_API_KEY/SECRET` in Vercel (base URL defaults to the test host). Evaluation tier = 50 req/day: results cached in Supabase `stay_search_cache` (24h TTL), 45-call/day guard falls back to mock whose `mock-` ids re-label cards as test data. v1 is availability-only for amenities/photos; distance-from-center is COMPUTED (haversine vs the searched point, on cards + in offers JSON for the model). Result pipeline (all unit-tested): PRICE-first budget terciles (≥4★ guard on luxury — real categories don't track price, "€42 4★") → median-relative DISTANCE tier (near half of the set, nearest-first backfill to 5; `distanceFilter:"any"` tool param skips; user-named area moves the searched point itself) → 8-card cap → worth-it DEAL detection (far offer ≥30% under shown same-star median, split into the tool JSON as `deal`, offered via one sanctioned teaser sentence — the only question allowed in a cards message).
- **Preferences (ask-don't-assume)**: the concierge records ONLY explicitly-stated/confirmed preferences via the `remember_preference` tool — scope `stable` → `users.preferences` (crosses trips, in the static prompt's profile line), scope `trip` → `trips.preferences` jsonb (dynamic tail; dies with the trip). Known preferences open as questions ("גם הפעם?"), never silent filters. The old keyword auto-learner (`lib/preferences.ts`) is deleted — do not reintroduce inference-based preference learning.
- **Delimiter blocks**: the model appends ONE `<<OPTIONS>> | <<FLIGHTS>> | <<STAYS>> … <<END>>` block per message. Frontend `components/chat/chat-client.tsx` parses via `displayText` (strips from the first `<<`, so raw blocks never leak) + tolerant `blockRaw`. Card views live in `components/chat/message-parts.tsx` (shared by the real chat AND the landing demo, so they can't drift). Shared `CardSelect` action posts a structured choice as the user's message.
- **Design system**: time-of-day phase tokens (`--c-*`) under `[data-phase]` in `app/globals.css`; 5 phases (sunrise/morning/midday/sunset/night) set from local time (flash-free via a `next/script` inline init). Suez One (display) + Heebo (body). Chat is RTL-first with per-message `dir="auto"` + `unicode-bidi: plaintext`; times/codes/prices stay LTR. Every animation is gated under `prefers-reduced-motion`.
- **Brand**: `components/brand/` (cloud mark + `Lockup`); favicon `app/icon.svg` + `app/apple-icon.tsx`; social image `app/opengraph-image.tsx` (fixed sunset palette).

## Recently shipped
- **Consistency pass (2026-07-21 pm)** — unified design across every surface (landing + chat + cards + modal + sidebar, desktop + mobile) with no visual jumps, WITHOUT touching the signature (living phase-sky + organic cloud bubbles stay as-is). New token layer in `globals.css`: a **cloud radius family** (`--radius-inset` 12 / `--radius-card` 16 / `--radius-panel` 24, exposed as `rounded-inset/card/panel`) with concentric "puff" nesting — a card (16) opening a modal (24) now steps gently instead of the old 12→24 jump; plus two **elevation tokens** (`--shadow-rest` / `--shadow-float`). Applied everywhere: all cards/rows/room-cards/gallery-images/user-bubble → `rounded-card`; modal/composer/demo-panel → `rounded-panel`; nested thumbs → `rounded-inset`. **Control roles unified**: one CTA (`rounded-full px-7 py-3.5 text-base` — landing hero+footer+sign-in gate now identical; New Trip joined the accent-pill family), one pill (`px-4 py-2 text-sm`), one chip (`px-3 py-1.5 text-xs` — sort chips + show-more + board pills). Dead legacy tokens (`--sky-deep/teal/cloud/slate/background/foreground/ink`) fully removed — globals.css is legacy-free; the mobile drawer scrim now uses the detail modal's phase-tinted treatment (one scrim language). Live-verified via Chrome (desktop, computed styles: both CTAs `14px 28px`/`16px`, card 16px, composer+modal 24px, phase-tinted scrim) + Browser pane (mobile 375px: no content overflow, CTA identical; only ambient sky-clouds extend, clipped). NOTE: claude-in-chrome embeds pages at a fixed 1280 viewport (can't emulate mobile); the Browser pane resizes reliably but can't sign in (auth-gated) and can't screenshot the WebGL hero — mobile chat state relies on token-only changes with breakpoint classes untouched.
- **Show-more hardening + flight hearts (2026-07-21 pm)** — from Max's live round: **MORE ticket persisted** (survives the new-trip navigation remount + reloads — the missing-button bug; the model never sees it: `stripMoreBlock` at the model-history seam); **session-wide show-more exclusion** (`collectShownStayIds` seeds the seen-set from every STAYS block in the conversation — batches can never re-serve a hotel from ANY earlier stack; same-city-new-dates over-exclusion accepted by design); **flight hearts** (generic `toggleFavorite(itemType, item)`; HeartButton beside the flight price; `item_type:'flight'` persists normally with the mock provider tag; drawer's "טיסות שאהבתי" rows show airline · route · price, unheart-only — no flight detail surface yet). check_favorites + taste layer remain stays-only by design. Live-verified via Chrome: reload-surviving button, exclusion-correct replacement batch (captured request/response), flight heart → drawer row. Known cosmetic: React #418 hydration warning on chat reload (pre-existing, benign — worth a look someday).
- **Hearts & junction round (2026-07-21)** — Max's product direction, full stack: **generic favorites** (`trip_favorites` table — item_type-generic from day one, stays now, flights/attractions plug in with zero schema changes; snapshot jsonb + lang; SQL handed over in chat, graceful 503 until run) + `GET/POST/DELETE /api/trips/[id]/favorites` + `HeartButton` on stay cards/detail modal (optimistic state owned by ChatShell) + sidebar favorites drawer (grouped by type, tap → modal, unheart inline); **show-more-replace** (server-authored `<<MORE>>` block — model never sees it; `/api/stays/more` → `peekStays` cache-only + `nextStayBatch` band→sort→minStars→next-5; REPLACES the stack; honest exhaustion/stale lines; badge belongs to batch 1; MORE not saved to history — button dies on reload by design); **what-now junction** (every component close → ✓ + pills from live state: 🤍 favorites (N) / next open piece / סיימתי בינתיים; nearly-complete note; replaces circle-back prose; the ONLY two sanctioned emoji: the 🤍 pill + one 😔 for a vanished hearted hotel); **check_favorites tool** (on-demand availability recheck before surfacing hearts — one quota-guarded by-codes call, hbh| cache; honest statuses incl. checked:false = no claims; find-similar path seeds from the vanished hotel's traits); **learning layer** (hearts = strongest taste tier > regrets > phrasing; dynamic-tail summary; "בהתאם למה שאהבת" framing MANDATORY when hearts influence a pick; 3+-heart patterns need ONE confirmed yes before remember_preference stable — hearts never auto-cross trips); **P0 auto-advance fix** (cards exist only on the latest message — declined refinement re-presents; settled = PICKED, never recommended).
- **Post-incident restoration round (2026-07-21)** — the 2026-07-21 crashes were environmental (API credit depletion; the reverted feature code was healthy). Restored the full refinement round (revert of d5fac52) plus upgrades: **unified error path** (`<<ERROR>>` in-stream marker → the one branded, localized bubble + retry for BOTH failure classes; an errored turn never navigates away — kills the vanishing bubble; `hasErrorMarker` in `lib/chat/blocks.ts`); **luxury precision question** (explicit luxury phrasing replaces the budget menu with "רק 5 כוכבים, או גם 4 איכותיים?" pills → route-level `minStars: 5`; honest fallback via `lib/stays/present.ts` `applyMinStars` — inventory can't meet the bar → full set + `minStarsNote` the model relays, inventory-scoped); **superlative honesty** (any "הרמה הגבוהה ביותר" claim is scoped to "במלאי הזמין לי"); **country-narrowing guard** (search_stays never runs on a country name/centroid — narrowing first; listening skips the BUDGET question, never destination narrowing). Discipline: zero-trip gate (Tier U + the three breaking messages on a preview deployment) BEFORE merge to main; live browser verification on cloud9app.io before "done".
- **Refinement round (2026-07-21 pm)** — from Max's live test of the six-fix delivery: **budget listens first** (explicit words = answer → set level, acknowledge in passing, record trip-scope, never re-ask; indirect-but-real signal → short two-option confirm; the ask-don't-assume section now defines LISTENING vs ASSUMING); **smart sort** (route-level `sortBy` price/premium/distance on search_stays, chosen ONLY from the user's own words; chat re-sort re-calls the cached search; StayQuery/provider untouched); **recommendation surface** (`recommendedId` in the STAYS block, parser accepts only a SHOWN offer id → badge "ההמלצה שלי" + accent border, floated first); **sort chips** (`StayStack` + `StaySortChips`: הכי מתאים לי / זול→יקר / יקר→זול / קרוב למרכז — pure client re-sort via unit-tested `sortStayOffers`, chips only when >1 card, state dies with the next message); **room mini-gallery** (per-room image cap 3→8 + `CONTENT_VERSION` stamp — older cache entries refetch once, the generalized refetch trigger; shared `SnapGallery` extracted from the hotel gallery; tap a room's photo/name → inline gallery; `content_api_fields` diag now records roomsWithImages/maxImagesPerRoom).
- **First-user fixes + hotel-by-name round (2026-07-21)** — from the first real external session (Max's cousin, mobile): pre-stream chat failures now self-report to diag_events (`chat_request_error` + stage — the error-bubble blindspot); retry affordance on the connection-error bubble (localized, resends the failed message; error bubbles are local-only); language-agnostic sentence-spacing rule (own prompt bullet + deterministic `fixSentenceSpacing` display guard in `lib/chat/blocks.ts`); inventory-honest phrasing rule; room photos in the detail modal (Content API `roomCode` association → `HotelContent.roomImages` → exact-code join onto captured rooms; pre-round cache entries refetch once); **hotel-by-name search** (`searchStayByName`: optional `hotelName` on search_stays; geo-results match first (free) → permanent destination/name index (Content API, `__destinations__`/`__hotelindex__*` cache codes) → one availability-by-codes call (`hbh|` key, quota-counted); honesty statuses available/unavailable/not_in_inventory/lookup_failed with a hard never-fake rule; mock mirrors all paths via "closed"/"nowhere" name probes). Reviews verdict CLOSED: no review data in the test env's Content API either (CONFIRMED ×3) — display-when-present code stays, first-party reviews are THE path.
- **Design polish round (2026-07-21)** — cloud-soft scrollbars (`.scroll-soft`/`.scroll-hide`), gallery full-bleed + hidden scrollbar; PICKED & LOCKED: close button = subtle phase-tinted cloud (`CloudCloseButton`), gallery nav = snap dots below (active dot elongates). Variant props + `/preview/modal` removed. Broader sweep (pill unification, mobile card width, card motion via transitions-dev tokens, spacing rhythm, chat scrollbar, modal close animation) still PROPOSED, not approved.
- **Detail layer round (2026-07-20 pm)** — hotel detail modal (phase-tinted scrim, gallery, amenities, room mini-cards with board-variant price pills; tap a stay card to open; `/api/stays/detail`); rooms captured from the search response at zero extra API cost (`hbrooms|` rows, option A — booking round re-verifies fresh); Content API client with permanent `hotel_content_cache` (lazy on first open; facilities catalog cached once; `content_api_fields` diag answers the TripAdvisor question on first live fetch); `get_hotel_details` tool (room answers from data, never memory); two selection paths (card = standard room stated honestly; modal = exact room+board); temp `/preview/modal` route pending Max's review. Playwright browser verification via the webapp-testing skill (Node variant — no local Python).
- **Consultation round (2026-07-20 pm)** — median-relative distance tier + worth-it deal v1 (see Architecture); style C open door (invitation statement on every consultation question); ask-don't-assume preference policy + `remember_preference` tool with stable/trip scopes (keyword auto-learner deleted); reviews foundation designed (schema handed over, BOTH/AND strategy).
- **Real-data round (2026-07-20)** — Hotelbeds live: relative dates echo their resolution visibly (statement, day-first + nights; question only when genuinely ambiguous); select-post prefixes ("בחרתי"/"Selected") decide reply language before word counting (Milano fix); hotel consultation prompt (area question, trade-off advising, "הכי כדאי" fit-based recommendations — never price alone); price-first budget terciles with luxury star guard.
- **Unit test tier (2026-07-16 pm)** — vitest, 44 tests: block parsers + cards-win-ties precedence (extracted to `lib/chat/blocks.ts`), language policy, calendar math (`lib/chat/dates.ts`), mock determinism + Hotelbeds mapper. `npm run test:unit` on every change.
- **Post-selection round (2026-07-16 pm)** — offer cards render only on the latest message (pick → cards gone; history keeps summaries; product decision: "what did I see?" = conversational re-search); regret reopens the choice — one clarifying question → refined re-search → fresh cards, learned preference is standing for the trip.
- **Streaming & polish round (2026-07-16)** — `StreamedText` smooth reveal (words flow from first token; ~300ms drain, snap on stream end, reduced-motion aware; streaming was verified working end-to-end — the fix is perceptual); non-Hebrew turns get a concrete language directive (verbatim message quote, English fallback); 3+-destination titles use a regional name or "A, B & more".
- **Trip-integrity round (2026-07-08 pm)** — latest-40 history window (context-bleed root cause); offers-never-text universal card rule + one-leg-at-a-time for multi-destination stays; multi-destination auto-titles with custom-rename lock; live night count on the calendar + mismatch cross-check; open-pieces tracking (circle back to deferred flights/stays); trip rename UI + `PATCH /api/trips/[id]`.
- **Language & behavior round (2026-07-08)** — deterministic reply language (see Architecture); calendar sends with a non-question lead-in (block-only messages render without an empty bubble); user-facing dates are DD-MM-YYYY day-first (tools stay ISO); pills for real choices carry the actual alternatives (never Yes/No); a decline redirects instead of closing the conversation.
- **Date-picker block** — `<<DATES>>` block (`{"lang","mode":"single"|"range","min"?,"max"?}`) → `DateCalendar` in `message-parts.tsx`: phase-tokened month calendar, he/en via `Intl`, RTL-aware, past dates unselectable (min clamped to today client-side), Confirm posts `בחרתי תאריכים: X עד Y` / `Selected dates: X to Y`. Only actionable on the latest message (like pills). Prompt: calendar for concrete dates, OPTIONS pills for vague timing.
- **Stay agent** — hotel search mirroring flights (mock provider, `StayCard`, he/en localized via neutral keys).
- **Card actions** — shared `CardSelect` (Select → structured user message); expandable flight details (connecting flights only; direct flights render flat).
- **Behavior tuning** — professional concierge tone (no slang/emoji, both languages); one question per turn but NEVER decides material choices (offer them as options); offers always re-sent as card blocks (never text lists); confirm carried-over context on a destination switch (Yes/Change pills); guided narrowing for undecided users; fixes for raw-block leak, mixed-language leak, and past-date invention.
- **Brand system** — mark, lockups (horizontal for header, stacked for og), living float (accent-bound), favicon + og image.

## Open TODOs — RANKED (fresh-session starting point)
1. **Design sweep** (proposal in chat 2026-07-21, awaiting approval): pill unification to two roles, full-width cards on mobile, card enter stagger + modal close animation + motion-token adoption (transitions-dev), spacing rhythm, chat scrollbar.
2. **Attractions agent** (NEW, unspecced): third agent alongside flights/stays — spec round needed.
3. **Flights provider decision**: Duffel went silent (decision day was 2026-07-09); Amadeus Self-Service test account is the fallback path. Needs Max's call.
4. **Email auth** (NEW, unspecced): sign-in beyond Google OAuth. Touches the no-touch auth zone — plan carefully.
5. ~~Content-API review coverage~~ — RESOLVED 2026-07-21: no review fields in the test env (CONFIRMED ×3, see `memory/first-party-reviews`); display-when-present code stays; first-party reviews are the path.

## Older open items (details in `memory/`)
- Supabase migrations pending (SQL handed over 2026-07-20): `trips.preferences` jsonb (trip-scoped preference storage; degrades gracefully until run) + `hotel_reviews` table (first-party verified reviews foundation — BOTH/AND strategy with TripAdvisor cold-start, see `memory/first-party-reviews`; collection/display are post-booking phases) + `diag_events` table (runtime self-reporting; logDiag degrades to console-only until run).
- Reviews verdict CLOSED (2026-07-21, CONFIRMED ×3): NO review data in availability (Sofia run) NOR the Content API (content_api_fields diag, live opens). First-party verified reviews are THE path; `reviewScore`/`reviewCount` stay display-when-present in code.
- Supabase migration pending: `alter table trips add column if not exists name_is_custom boolean not null default false;` (locks custom titles; everything works without it, but auto-titling may overwrite renames until it runs).
- Supabase migration pending: `stay_search_cache` table (SQL handed over in chat) — required before flipping `STAY_PROVIDER=hotelbeds`, else every search burns live quota.
- Supabase migration pending (SQL handed over 2026-07-20 pm): `hotel_content_cache` table — content fetches fail gracefully (modal shows no photos/amenities) until it runs.
- Temp `/preview/modal` route awaiting Max's review — delete after.
- Content enrichment largely SHIPPED via the detail layer (photos/description/amenities in the modal). Remaining: TripAdvisor-in-Content-API verdict (auto-answers via `content_api_fields` diag on the first live modal open) and surfacing amenities/photos on the CARDS themselves (currently modal-only).
- Hotelbeds keys pending (manual review, days) — on arrival: keys → Vercel env, run cache SQL, flip `STAY_PROVIDER=hotelbeds`, live test.
- RTL/LTR by user language preference (build with onboarding).
- Room selection within a stay (needs the real stay provider).
- Automated tests for critical flows (pre-launch, deliberate task).
- Pre-launch: footer legal links; connect the cloud9app.io domain.
- External: Duffel reply — decision Thu 2026-07-09 (else follow-up + Amadeus test account).

## Installed skills (`.claude/skills/`, discoverable from session start)
- **Playwright MCP** (`.mcp.json`, added 2026-07-21): drives a real browser at ANY viewport — closes the mobile blind spot the other browser tools couldn't (claude-in-chrome is locked at 1280px; the Browser pane can't sign in). Verified working: rendered cloud9app.io at a true 390px and screenshotted the hero (which the Browser pane couldn't — it times out on the WebGL hero). Two caveats: (1) the `mcp__playwright__*` tools load at SESSION START, so a new session gets them (with a one-time approval prompt for the project MCP server) — they are NOT live in the session that added `.mcp.json`; (2) config is persistent + headed (no `--isolated`) so signed-in mobile CHAT is reachable — but Max must sign in with Google ONCE in the Playwright browser (credentials can't be entered by the session); after that the profile persists and auth'd mobile chat is verifiable directly.
- **webapp-testing** (Anthropic): Playwright-driven browser verification — after UI/flow changes, drive the app (screenshots, DOM checks, console logs) BEFORE handing to Max. Constraint: production is auth-gated and worktrees have no `.env.local`, so this covers the landing page + non-auth pages + component states on the local dev server. SIGNED-IN chat flows ARE now verifiable live: Max's Claude-in-Chrome extension (connected 2026-07-21) lets the session drive his real signed-in browser on cloud9app.io — used for the restoration round's live gate. Note: Vercel PREVIEW deployments cannot complete sign-in (NextAuth/OAuth pinned to production domains — verified `error=Configuration`), so live-model gates run on production post-deploy with instant-revert as the fallback. One-time local deps on first use: `pip install playwright` + `playwright install chromium`.
- **diagnosing-bugs** (mattpocock): feedback-loop-first diagnosis (tight pass/fail signal, ranked hypotheses, bisection). MERGES with — never replaces — the investigate→findings→approval discipline: the skill governs HOW to investigate; findings still go to Max before any fix ships.
- **supabase-postgres-best-practices** (Supabase): apply when designing schemas/migrations/RLS or reviewing DB performance; every table handover gets checked against it (grants + RLS + FK indexes are the recurring gaps).
- **transitions-dev** (Jakubantalik/transitions.dev): 27 portable CSS transitions + a motion-token scale (--duration-*/--ease-*/--distance-*/--scale-*/--blur-*), every snippet reduced-motion guarded. Use for any open/close/swap/reveal motion and to tokenize ad-hoc durations (transitions refine). Design-system note: our phase tokens own COLOR; transitions-dev tokens own MOTION — never mix the two vocabularies.
- Deferred by decision (do NOT install unprompted): TDD skill, improve-codebase-architecture.

## Working process
- Non-trivial features: short plan → **WAIT for approval** → implement.
- Verify locally: `npm run test:unit` (Tier U — free, ~5s, part of the definition of done on EVERY change) + type-check + `npm run build` + preview DOM/computed-style checks. NOTE: the harness preview can't render animations, WebGL, or screenshots — flag those for the user's live review.
- Tier B (behavior harness, "robot Max" — multi-turn conversations vs the chat route with model-call cost) is an approved DESIGN but shelved: implement only if the user pulls the trigger (details in `memory/robot-max-behavior-suite`).
- Small focused commits; push to `main`. Temp preview/verification routes are deleted before their commit.
- EVERY Supabase table handover includes GRANT statements (at minimum `grant all on table X to service_role;`) — missing grants on `stay_search_cache` silently killed the cache with 42501 on every write from creation until 2026-07-20 (every search burned live quota). Grants audit ran 2026-07-20.
- Do not touch without asking: auth, DB, API-route logic internals, provider-layer signatures.
